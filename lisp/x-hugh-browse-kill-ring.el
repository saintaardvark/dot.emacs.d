;;; -*- lexical-binding: t -*-
;; x-hugh-browse-kill-ring --- Browse-Kill-Ring stuff

;;; Commentary:
;; Commentary goes here.

;;; Code:

(use-package browse-kill-ring
  :ensure t)

(provide 'x-hugh-browse-kill-ring)
;;; x-hugh-browse-kill-ring.el ends here
