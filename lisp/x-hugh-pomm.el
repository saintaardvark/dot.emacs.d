;;; x-hugh-pomm --- Pomm package

;;; Commentary:
;;; Let's try out pomm

;;; Code:

(use-package pomm
  :ensure t
  :config
  (progn
    (setq alert-default-style 'libnotify))
  )

(provide 'x-hugh-pomm)

;;; x-hugh-pomm ends here
